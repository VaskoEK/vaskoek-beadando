export type Recipe = {
    name: string,
    id: number,
    category: string,
    imagePaths: string[],
    shortDescr: string,
    longDescr: string,
    ingredients: string[],
    directions: string[]
}

